#
# Just setup the tables for the test.
#

create table test (
        user    char(10) primary key,
        age     int,
        phone   char(20)
)
\p\g


create table test2 (
        user    char(10) not null,
        fullname    char(30)
) \p\g

